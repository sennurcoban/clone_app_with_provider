import 'package:flutter/material.dart';

class SavedCouponsScreen extends StatelessWidget {
  const SavedCouponsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
